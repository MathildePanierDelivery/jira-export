# ── Données sensibles : NE JAMAIS committer ──
jira_cache.pkl          # tous les worklogs de l'équipe
absences_2026.xlsx      # congés / maladie nominatifs (données personnelles)
absences.xlsx           # ancien format absences

# ── Tokens / secrets (au cas où) ──
*.token
.env

# ── Python ──
__pycache__/
*.pyc
.venv/
